. $( cd -- "$( dirname -- "${BASH_SOURCE[0]}" )" &> /dev/null && pwd )/../_config.sh
SCRIPT_DIR=$( cd -- "$( dirname -- "${BASH_SOURCE[0]}" )" &> /dev/null && pwd )

export FUNCTION_NAME=smz-gcp-012-functions-env
export TOPIC_NAME=smz-gcp-012-functions-env-topic
